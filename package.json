{
  "name": "review-triage-agent-skills",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "node scripts/provision.mjs",
    "verify": "node scripts/verify.mjs",
    "setup": "npm run deploy && npm run verify"
  },
  "dependencies": {
    "@zoowork-ai/sdk": "0.7.0"
  },
  "description": "Skill pack for a System One review-triage digital employee on ZooWork Agent Runtime"
}